{
  "name": "rundea",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["apps/api", "apps/web", "packages/*"],
  "engines": {"node": ">=24"},
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev:api": "npm run dev -w @rundea/api",
    "dev:web": "npm run dev -w @rundea/web"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "tsx": "4.20.5",
    "typescript": "5.9.2"
  }
}
